  <div class="row">
    <div class="offset-md-3 col-md-6">
        <% if @user.img.present? %>
          <h3 class="text-center mb-5 mt-3"><%= image_tag @user.img.url, class: "pic", style: "width: 40px;, hight: 40px;" %>:<%= link_to @user.name, user_path(@user) %>さんの記事</h3>
        <% else %>   
          <h3 class="text-center mb-5 mt-3"><img class="rounded img-fluid" src="<%= gravatar_url(@user, { size: 30 }) %>" alt="">:<%= link_to @user.name, user_path(@user) %>さんの記事</h3>
        <% end %>
      
      <p>タイトル:</p>
      <%= @post.title %>
      <p class="mt-5">内容:</p>
      <p><%= simple_format(@post.content) %></p>
        <div class="thumbnail">
          <% if @post.image.present? %>
            <p class="text-center"><%= image_tag @post.image.url, class: "pic", style: "width: 200px;, hight: 200px;" %></p>
          <% end %>
        </div>
    </div> 
    
  </div>
  
  
  <div class="row">
    <div class="col-md-2 mt-5">
      <p class="mb-0">コメントする</p>
        <%= form_with(model: [@post, @comment], local: true) do |form| %>
          <%= form.text_area :text, placeholder: "コメント", rows: "2" %>
          <%= form.submit "送信" %>
        <% end %>
    </div>
    <div class="offset-md-2 col-md-4">
        <div class="text-center mt-5">
          <% if current_user && @posts %>
            <%= render 'favorites/favorite_button', post: @post %>
            <button class="btn btn-outline-success" type="submit"><%= link_to '編集する', edit_post_path(@post) %></button>
            <button class="btn btn-outline-danger" type="submit"><%= link_to '削除する', @post, method: :delete, data: { confirm: '本当に削除してよろしいですか？' } %></button>
          <% else %>
            <%= render 'favorites/favorite_button', post: @post %>
          <% end %>
          <div class="mt-3">
              <%= link_to '戻る', root_url %>
          </div>
        </div>
    </div>
    <div class="offset-md-1 col-md-2 mt-5">
      <%= link_to '👍:この記事のいいね', likers_post_path(@post) %>
    </div>
  </div>   
        
  <div class="row">  
    <div class="offset-md-4 col-md-4 mt-5">
      <h5 class="ml-4">コメント一覧</h5>
        <% if @comments %>
          <% @comments.each do |comment| %>
          <p>
            <strong><%= link_to comment.user.name, "/users/#{comment.user_id}" %>：</strong>
            <%= comment.text %>
          </p>
          <% end %>
        <% end %>
    </div>
  </div>
    